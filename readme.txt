== doesnt compare if a string or number
=== also checks data type

document.querySelector can be used for both id, class etc
just use #, . etc to denote id or class etc

array syntax: 
let varName = [elements]
varName.push(value) to add new elements
varName.pop() to pop last element

for syntax:
for(let count = 1; count < n; count += 1) {
    loop body
}